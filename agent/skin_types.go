package agent

import "github.com/reallyoldfogie/mc-agent/models"

// ProfileProperty is the exported skin property type.
type ProfileProperty = models.ProfileProperty

// profileProperty keeps internal helpers working with the shared type.
type profileProperty = models.ProfileProperty
